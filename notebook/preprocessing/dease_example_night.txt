# ---------------------------
# Load libraries
# ---------------------------
library(tidyverse)
library(mgcv)
library(tibble)
library(ggtext)

# ---------------------------
# Load DEASE CSV
# ---------------------------
df <- read_csv("C:/Users/percy/Documents/studies/visual_studio_code/PhD-chapter2-data-fusion-full-diurnal-lst/output/csv/dease_all_dates.csv")

# ---------------------------
# Filter for direction and temporal
# ---------------------------
df <- df %>%
  filter(direction == 315, temporal == "night")

# ---------------------------
# Keep raw LST data per date
# ---------------------------
df_raw <- df %>%
  group_by(date) %>%
  arrange(distance, .by_group = TRUE) %>%
  select(date, distance, mean_lst) %>%
  ungroup()

# Order from oldest to newest
df_raw$date <- factor(df_raw$date, levels = c(
  "3/12/2022",
  "25/12/2022",
  "27/12/2022",
  "7/01/2023",
  "27/01/2023"
))

date_colors <- c(
  "3/12/2022"  = "gray10",
  "25/12/2022"  = "red",
  "27/12/2022" = "gray35",
  "7/01/2023"  = "gray60",
  "27/01/2023" = "gray85"
)

# Custom labels with markdown for the legend
date_labels <- c(
  "3/12/2022"  = "3/12/2022",
  "25/12/2022" = "<span style='color:red;'>25/12/2022</span>",  # text red
  "27/12/2022" = "27/12/2022",
  "7/01/2023"  = "7/01/2023",
  "27/01/2023" = "27/01/2023"
)

# ---------------------------
# Optional: define color palette (automatic for all dates)
# ---------------------------
# If you want custom colors for specific dates, replace scale_color_viridis_d() with scale_color_manual()
color_palette <- viridis::viridis(length(unique(df_raw$date)))

# ---------------------------
# Plot smoothed curves
# ---------------------------
p <- ggplot(df_raw, aes(
  x = distance,
  y = mean_lst,
  color = date,
  group = date
)) +
  geom_line(size = 1.2) +
  scale_color_manual(values = date_colors, labels = date_labels) +  # use labels here
  scale_x_continuous(
    limits = c(0, 31),
    breaks = seq(0, 31, by = 5),
    minor_breaks = seq(0, 31, by = 1),
    expand = c(0, 0)
  ) +
  scale_y_continuous(
    limits = c(10, 40),
    breaks = seq(10, 40, by = 10),
    minor_breaks = seq(10, 40, by = 1),
    expand = c(0, 0)
  ) +
  labs(
    title = "Nighttime LST Profile — Bearing 315° (NW)",
    x = "Distance from the Image Centre Point (km)",
    y = expression(LST~(degree*C)),
    color = "Date"
  ) +
  theme_minimal() +
  theme(
    legend.position = "right",
    legend.title = element_text(size = 18),
    legend.text = element_markdown(size = 18),  # <-- use markdown for colored text
    axis.title.x = element_text(size = 18, face = "bold"),
    axis.title.y = element_text(size = 18, face = "bold"),
    axis.text.x  = element_text(size = 16),
    axis.text.y  = element_text(size = 16),
    plot.title = element_text(hjust = 0.5, size = 24, face = "bold"),
    panel.background = element_rect(fill = "white", color = "grey"),
    plot.background  = element_rect(fill = "white", color = NA),
    panel.border     = element_rect(color = "grey", fill = NA, size = 1),
    plot.margin = margin(5, 10, 5, 0),
    panel.grid.minor.x = element_line(colour = "grey85"),
    panel.grid.major.x = element_line(colour = "grey75")
  )


# Display plot
print(p)

# ---------------------------
# Save figure
# ---------------------------
ggsave(
  filename = "C:/Users/percy/Documents/studies/visual_studio_code/PhD-chapter2-data-fusion-full-diurnal-lst/output/plots/night_direction_315.png",
  plot = p,
  width = 12,
  height = 6,
  dpi = 300
)

